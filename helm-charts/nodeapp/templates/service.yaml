apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-nodejs
spec:
  type: {{ .Values.service.type }}
  selector:
    app: nodejs
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 3000
